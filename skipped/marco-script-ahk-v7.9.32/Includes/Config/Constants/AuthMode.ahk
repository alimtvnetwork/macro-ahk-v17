; Config\Constants\AuthMode.ahk - Enum-like static class for authentication modes
; Used by CreditStatus config and combo.js token resolution

class AuthMode {
    static COOKIE_SESSION := "cookieSession"
    static TOKEN          := "token"
}
